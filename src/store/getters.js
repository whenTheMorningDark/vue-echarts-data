/* eslint-disable semi */
/* eslint-disable indent */
const getters = {
	currentTarget: (state) => state.echart.currentTarget,
	echartArr: (state) => state.echart.echartArr,
};
// eslint-disable-next-line eol-last
export default getters;
